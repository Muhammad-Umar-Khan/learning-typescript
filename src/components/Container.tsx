import { ContainerProps } from "../const/types";

const Container = ({ style }: ContainerProps) => {
  return (
    <div style={style}>
      <text>Hello</text>
    </div>
  );
};

export default Container;
